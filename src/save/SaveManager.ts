import { DEFAULT_SAVE, type SaveData } from './SaveTypes';
const KEY='delibuddies.save.v1';
export class SaveManager { load():SaveData{try{const raw=localStorage.getItem(KEY);if(!raw)return {...DEFAULT_SAVE,discoveredRoutes:[]};const v=JSON.parse(raw) as Partial<SaveData>;return {...DEFAULT_SAVE,...v,version:3,musicEnabled:v.musicEnabled!==false,sfxEnabled:v.sfxEnabled!==false,discoveredRoutes:Array.isArray(v.discoveredRoutes)?v.discoveredRoutes.filter(x=>typeof x==='string'):[],missionAttempts:Number.isFinite(v.missionAttempts)?Math.max(0,Math.floor(v.missionAttempts!)):0};}catch{return {...DEFAULT_SAVE,discoveredRoutes:[]};}} save(p:Partial<SaveData>){const next={...this.load(),...p,version:3} as SaveData;try{localStorage.setItem(KEY,JSON.stringify(next));}catch{}return next;} }
