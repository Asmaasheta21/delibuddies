import { Mission } from './Mission';
export class CakeDeliveryMission extends Mission { static readonly duration=240; constructor(){super('delivery-01','DELIVERY 01');this.reset();} reset(){this.status='ACTIVE';this.timer=CakeDeliveryMission.duration;this.result=null;} tick(dt:number){if(this.status!=='ACTIVE')return;this.timer=Math.max(0,this.timer-Math.min(dt,1/12));if(this.timer===0)this.fail('TIME');} fail(reason:'TIME'|'CAKE'){if(this.status!=='ACTIVE')return;this.status='FAILED';this.result={reason,score:0,stars:0,timeTaken:CakeDeliveryMission.duration-this.timer,condition:0};} complete(condition:number){if(this.status!=='ACTIVE'||condition<=0||this.timer<=0)return false;const whole=Math.floor(this.timer),stars=condition>=80&&whole>=75?3:condition>=50&&whole>=30?2:1;const score=5000+whole*10+Math.round(condition)*25;this.status='SUCCESS';this.result={score,stars,timeTaken:CakeDeliveryMission.duration-this.timer,condition};return true;} get objective(){return this.status==='ACTIVE'?'PICK UP THE CAKE':'DELIVER THE CAKE';} }
